[build]
builder = "nixpacks"
buildCommand = "npm run setup && npm run build"

[deploy]
startCommand = "node server/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "palm-investment-os"
